$(document).ready(function() {
  // Slider sur section article
  $('.article-commentaire .flexslider').flexslider({
    animation: "slide",
    controlNav: "thumbnails"
  });

  // Slider sur section rhizome
  $('.article-rhizome .flexslider').flexslider({
    animation: "slide"
  });
});